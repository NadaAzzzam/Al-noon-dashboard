# Server port (default: 4000). On Render, PORT is set automatically.
PORT=4000

# MongoDB connection string. Use MongoDB Atlas (https://www.mongodb.com/atlas) for production.
MONGO_URI=mongodb://127.0.0.1:27017/al-noon-node

# JWT signing secret. Use a long random string in production (e.g. `openssl rand -hex 32`).
JWT_SECRET=change-me
JWT_EXPIRES_IN=1d

# Allowed CORS origin: your frontend URL. When backend serves the frontend (single deploy), use the same URL as the app (e.g. https://your-app.onrender.com).
CLIENT_URL=http://localhost:5173

# Optional: run without MongoDB for local dev (login: admin@localhost / admin123).
# DEV_WITHOUT_DB=1

# Optional: override admin seed defaults (used by npm run seed:admin).
# ADMIN_EMAIL=admin@localhost
# ADMIN_PASSWORD=admin123
# ADMIN_NAME=Admin
